/*
	This file sl_list.h
	single linked list header
	Author
	Created
*/
#include <stdio.h>
#include <malloc.h>
#include "sl_list.h"

int  main(int argc, char** argv)
{
   /* pslist a = NULL;
    int insert_values = 0;
    a = slist_new();
    //printf ("Size of your list = %d\n %p\n %p\n", a->list_size, a->head, a);
    slist_print(a);
    slist_insert(a, 7);
    slist_delete(a);
    slist_print(a);*/
	return 0;

}
